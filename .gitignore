# Build output — regenerated by `make`.
build/

# macOS Finder metadata.
.DS_Store

# Editor / IDE workspace files.
.vscode/
.idea/
*.swp
*~

# Claude Code local settings (per-machine, not shared).
.claude/

# Internal planning / roadmap notes (kept locally, not pushed).
STAGE8_PLAN.md
ROADMAP.md

# Compiled objects / binaries if they ever escape build/.
*.o
*.obj
*.elf
*.bin
*.dtb
*.pp
